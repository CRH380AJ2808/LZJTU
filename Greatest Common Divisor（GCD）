//C语言实现求两个数的最大公约数
//辗转相除法


int main()
{
    int r = 0;
    int m = 24;
    int n = 18;
    while (m % n)
    {
        r = m % n;
        m = n;
        n = r;
     }
    printf("%d\n", n);
    return 0;
}
